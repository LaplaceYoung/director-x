# Evolution Loop Backlog

## P0

None observed in the repository-only audit. A live-host installation test is still required before treating installation as proven.

## P1

1. Add host-signed receipt evidence (or an equivalent Codex-attested resolution record) for `directorx_decide_production`. Public prepare already persists and verifies its native canonical-brief/fingerprint gate; the remaining P1 is that its raw answer envelope is trusted from the host without an independently verifiable receipt.
2. Move public run-mode option-label-to-mode semantics out of caller-supplied decision application payloads into a host-trusted or independently canonical mapping.
3. Implement `directorx_build_rough_cut` for a selected single-source video with evidence-bound trim operations and public `post_production_edit` approval. Start, status, resume, decision, prepare, research, generation, candidate review, and recovery are complete migration slices.
4. Make the installed-cache and fresh Codex session test executable and record elapsed time to first successful call.
5. Convert edit, render, audit, repair, and delivery Skill handoffs to the public Facade contract as those Facades become available.

## P2

1. Reduce model-visible conversation text for legacy activity tools.
2. Add MCP Apps capability negotiation while retaining side Browser fallback.
3. Add an explicit installation troubleshooting page with actual host-state examples.

## P3

1. Consolidate specialist skill descriptions after usage evidence exists.
2. Add dashboard charts to the evolution report only if they improve a decision.
