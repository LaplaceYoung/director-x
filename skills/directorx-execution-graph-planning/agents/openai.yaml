interface:
  display_name: "Director X Execution Graph"
  short_description: "Plan and replan auditable video execution graphs"
  default_prompt: "Map this video task into a capability-filtered Director X execution graph and run it with evidence-backed node transitions."

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for capability routing, execution graphs, DX team dispatch, and artifact transitions."
