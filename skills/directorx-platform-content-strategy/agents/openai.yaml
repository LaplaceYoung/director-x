interface:
  display_name: "Director X Platform Strategy"
  short_description: "Adapt hooks, retention, covers, and publishing by platform"
  default_prompt: "Design a platform-native video strategy with the right hook, retention beats, duration, subtitles, cover, CTA, derivatives, and publishing package."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for intent, scripts, delivery promises, platform artifacts, derivative cuts, and publish packaging."
