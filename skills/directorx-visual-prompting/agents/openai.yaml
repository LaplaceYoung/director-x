interface:
  display_name: "Director X Visual Prompting"
  short_description: "Compile provider-ready image and video prompt packs"
  default_prompt: "Build per-shot image and video prompts from approved story, grounding, camera, continuity, reference roles, provider dialects, negative constraints, and repair evidence."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for shot grounding, visual prompt packs, model knowledge, provider routes, generation attempts, and candidate review."
