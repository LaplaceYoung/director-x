interface:
  display_name: "Director X Avatar & Lip Sync"
  short_description: "Plan safe, consistent presenter and dialogue videos"
  default_prompt: "Plan an evidence-backed avatar or talking-head workflow covering identity consent, voice, lip sync, gesture, continuity, background, and review."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for presenter assets, voice routes, shot planning, generation evidence, and continuity review."
