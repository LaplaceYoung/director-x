interface:
  display_name: "Director X Publish Packaging"
  short_description: "Package final videos for platform-ready publishing"
  default_prompt: "Create a platform-ready publish package with titles, cover direction, descriptions, tags, CTAs, derivative cuts, compliance notes, and stable filenames."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for verified delivery media, derivative cuts, publish artifacts, compliance evidence, and final approval."
