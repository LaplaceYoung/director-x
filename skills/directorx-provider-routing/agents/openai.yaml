interface:
  display_name: "Director X Provider Routing"
  short_description: "Route models by capability, quality, cost, and fallback"
  default_prompt: "Choose an evidence-backed image, video, voice, music, or editing route within the approved budget, with explicit capability probes and bounded fallbacks."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for provider catalogs, setup, probes, pricing, budget quotes, model approvals, jobs, and fallbacks."
