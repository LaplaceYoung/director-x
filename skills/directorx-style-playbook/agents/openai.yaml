interface:
  display_name: "Director X Style Playbook"
  short_description: "Compile a testable directing and visual style bible"
  default_prompt: "Compile the approved research and Director.md into precise visual, camera, lighting, motion, edit, typography, audio, originality, and review rules."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for research packages, Director knowledge, cinematic references, visual prompts, and downstream review gates."
