interface:
  display_name: "Director X Evidence Rough Cut"
  short_description: "Turn verified inactivity into an approval-gated first cut"
  default_prompt: "Have DX-Editor build an evidence-backed rough-cut draft, preserve context and caption timing, then request native approval before commit."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for evidence retrieval, rough-cut construction, timeline registration, and native approval."
