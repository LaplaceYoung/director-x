interface:
  display_name: "Director X Benchmark Runner"
  short_description: "Run repeatable video-agent quality and regression tests"
  default_prompt: "Create a bounded Director X benchmark suite, run every planned fixture and repeat, persist verifier evidence, and compare against the active baseline."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for benchmark suites, schedules, trials, verifiers, reports, and baseline governance."
