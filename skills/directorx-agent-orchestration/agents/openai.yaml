interface:
  display_name: "Director X Agent Orchestration"
  short_description: "Route DX specialists, tools, ownership, and handoffs"
  default_prompt: "Design the smallest capable DX production team, assign stage ownership and tools, then return executable subagent handoffs without nested delegation."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for DX team plans, role registration, dispatch evidence, and artifact handoffs."
