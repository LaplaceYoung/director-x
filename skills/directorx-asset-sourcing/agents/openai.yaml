interface:
  display_name: "Director X Asset Sourcing"
  short_description: "Source, verify, license, and register production assets"
  default_prompt: "Build an official-first asset plan, acquire usable media with provenance, audit rights and quality, and register only verified local assets."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for asset search plans, acquisition, rights evidence, quality audits, and canvas registration."
