interface:
  display_name: "Director X"
  short_description: "Start and supervise a durable AI video production run."
  default_prompt: "Use $directorx to create a 30-second product film and guide me through the few decisions that affect it."

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: "mcp"
      value: "directorx-production"
      description: "Bundled Director X MCP server for production Runs, native decisions, provider execution, canvas state, and final review."
