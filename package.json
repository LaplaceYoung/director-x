{
  "name": "@openmoss/directorx-codex-plugin",
  "version": "0.1.0",
  "private": false,
  "description": "Open-source AI video production harness for Codex with native Goals, dedicated DX agents, a live media canvas, provider routing, editing, and review.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test mcp/*.test.mjs runtime/*.test.mjs",
    "check": "node --check mcp/server.mjs && node --check scripts/validate-plugin.mjs",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "ci": "pnpm validate:plugin && pnpm check && pnpm test"
  }
}
