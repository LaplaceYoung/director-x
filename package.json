{
  "name": "@laplaceyoung/director-x",
  "version": "0.2.0",
  "private": false,
  "description": "A Codex-native video creation plugin with an infinite media canvas.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "directorx": "./scripts/directorx.mjs"
  },
  "scripts": {
    "check": "node --check scripts/directorx.mjs && node --check scripts/canvas-server.mjs && node --check scripts/analyze-video.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "ci": "npm run validate:plugin && npm run check && npm test"
  }
}
